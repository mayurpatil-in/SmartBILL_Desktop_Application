<UserControl x:Class="SmartBILL.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartBILL.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="10" BorderThickness="1" Style="{StaticResource WinBorderStyle}">
        
        
        <Grid>
            <Grid.ColumnDefinitions>
                <!--Navigation menu-->
                <ColumnDefinition Width="*"/>
                <!--Content section-->
                <ColumnDefinition Width="450"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1">
            <!--Year Panel-->
                <Border Margin="20,120,20,20" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Margin="10 88 10 20">
                        <TextBlock Text="Accounting Year" Foreground="White" TextAlignment="Center" FontSize="34" FontWeight="SemiBold"></TextBlock>
                        
                    </StackPanel>
                
                </Border>

                <!--Year Card-->
                <Border Margin="90 40 90 0" Style="{StaticResource CardBorder}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#d489ff" Offset="-0.2"></GradientStop>
                            <GradientStop Color="#7985ff" Offset="0.5"></GradientStop>
                            <GradientStop Color="#6ad8fd" Offset="1"></GradientStop>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <fa:IconImage Icon="CalendarDay" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="White" Opacity="0.9" Width="25" Margin="15 8 0 0"></fa:IconImage>
                        <fa:IconImage Icon="BuildingColumns" HorizontalAlignment="Right" VerticalAlignment="Top" Foreground="White" Opacity="0.9" Width="30" Margin="0,8,15,0"></fa:IconImage>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
